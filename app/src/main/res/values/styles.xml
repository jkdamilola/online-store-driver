<resources>

    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="@style/Theme.AppCompat.Light.NoActionBar" />
    <style name="AppTheme" parent="@style/AppBaseTheme" />
    <style name="MyDarkToolbarStyle" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="Animations.SmileWindow" parent="@style/Animations">
        <item name="android:windowEnterAnimation">@anim/bottom_to_top</item>
        <item name="android:windowExitAnimation">@anim/top_to_bottom</item>
    </style>
    <style name="Animations" />
</resources>
